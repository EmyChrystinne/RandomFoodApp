import React from 'react';

const FilteredOptions = ({ userName, recommendation, otherOptions }) => {
  return (
    <div>
      <h2>Opções Filtradas</h2>
      {/* Exibir recomendação e outras opções de restaurantes */}
      <button>Voltar para página inicial</button>
    </div>
  );
}

export default FilteredOptions;